import { createAction } from "@/data/actions/create-menu-item";
import { deleteAction } from "@/data/actions/delete-menu-item";
import { updateAction } from "@/data/actions/update-menu-item";

export const actions = {
    createAction,
    deleteAction,
    updateAction,
}